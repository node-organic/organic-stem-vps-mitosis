{
  "scripts": {
    "release-major": "npm version major && git push --tags",
    "release-minor": "npm version minor && git push --tags",
    "release-patch": "npm version patch && git push --tags",
    "postversion": "angel start remote mitosis {{{mitosis-name}}}"
  },
  "dependencies": {
    "organic-angel": "^0.3.2",
    "organic-plasma": "^2.1.0",
    "organic-plasma-channel": "^2.1.1",
    "angelabilities-package-scripts": "^0.1.0",
    "angelabilities-exec": "^1.0.0"
  }
}
